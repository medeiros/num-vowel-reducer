server.port=0
spring.application.name=num-vowel-reducer
eureka.client.serviceUrl.defaultZone=http://localhost:8010/eureka
eureka.instance.instance-id=${spring.application.name}:${spring.application\
  .instance_id:${random_value}